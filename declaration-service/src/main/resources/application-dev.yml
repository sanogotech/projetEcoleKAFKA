# Mode développement : pas de broker Kafka requis — événements en mémoire + logs
guce:
  mode: dev

spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
