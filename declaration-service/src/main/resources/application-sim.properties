# Simulation : Kafka reel (brokers dedies)
guce.mode=sim

spring.kafka.bootstrap-servers=${KAFKA_BOOTSTRAP_SERVERS:kafka-sim:9092}
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:h2:mem:guce_sim;DB_CLOSE_DELAY=-1;MODE=PostgreSQL}
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
