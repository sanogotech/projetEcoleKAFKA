# Production : Kafka reel, base PostgreSQL
guce.mode=prod

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/guce_declaration}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:guce}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:guce}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
