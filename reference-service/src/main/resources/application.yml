spring:
  application:
    name: reference-service

server:
  port: 8086

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
